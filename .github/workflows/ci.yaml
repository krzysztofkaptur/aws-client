name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "checkout"
        uses: actions/checkout@v7

      - uses: ./.github/actions/setup

      - name: "build the app"
        run: pnpm run build

  test:
    runs-on: ubuntu-latest
    steps:
      - name: "checkout"
        uses: actions/checkout@v7

      - uses: ./.github/actions/setup

      - name: "tests"
        run: pnpm run test:run

  lint:
    runs-on: ubuntu-latest
    steps:
      - name: "checkout"
        uses: actions/checkout@v7
        
      - uses: ./.github/actions/setup

      - name: "lint"
        run: pnpm run lint
      

